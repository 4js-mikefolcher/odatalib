# Genero build artifacts (regenerated by fglcomp/fglpkg)
*.42m
*.42f
*.42r
*.42x
*.42s
*.4tm
*.o
*.a

# fglpkg build/dist output
build/
dist/
*.fgz

# Local Claude Code settings (machine-specific permission allowlists)
.claude/
CLAUDE.md
.mcp.json

# GAS deployment descriptors — they bake in an absolute project path, so each
# checkout keeps its own. See README "GAS deployment" for the template.
examples/resources/*.xcf

# Editor / OS noise
.DS_Store
*.swp
*~

# Runtime scratch
*.log
